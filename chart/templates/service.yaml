apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  labels:
    app: {{ .Release.Name }}
spec:
  ports:
  - name: http
    port: {{ .Values.port.service }}
    targetPort: {{ .Values.port.application }}
  selector:
    app: {{ .Release.Name }}